name: MCP Weather Tools
description: Tools for weather alerts and forecasts
transport:
  type: streamable
  endpoint: https://mcpserverpocbhn.azurewebsites.net/mcp/stream
capabilities:
  tools:
    - name: get_alerts
      description: Get weather alerts for a US state
      inputSchema:
        type: object
        properties:
          state:
            type: string
            description: Two-letter US state code (e.g., CA, NY)
        required: [state]
    - name: get_forecast
      description: Get weather forecast for a location
      inputSchema:
        type: object
        properties:
          latitude:
            type: number
          longitude:
            type: number
        required: [latitude, longitude]
